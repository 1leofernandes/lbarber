// ANTIGO → NOVO
GET /agendamentos/barbeiro/:id → GET /agendamentos (requer auth)
POST /agendar → POST /agendamentos
GET /agendamentos/barbeiro/:id?data=... → GET /agendamentos/disponiveis?barbeiro_id=X&data=Y

Ação: Atualize no frontend as URLs de agendamentos.
